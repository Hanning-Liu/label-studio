# L4 绿植盆栽类别（2026-09-11）

- 基线：`6385c0e0ad8b860e80602174fe75c509eb3b396f`。
- 可执行代码：`533bcc0de5ea59bab82721c9cac122c64d394c34`。
- 镜像：`label-studio-window-l4:plant-533bcc0de`，镜像 ID `sha256:09828c47084dac8fb239b92a252497fdda9d06dfb3dab410e9f1ad7c563bec68`。

增加“绿植盆栽” (`potted_plant`)，追加在原“其他”分组末尾，沿用该组颜色。前后端、新建项目模板、类别定义和聚合支持 29 类，保留既有 8 组及原类别。盆栽沿可辨识的独立整体平面轮廓标注，类别说明见 `deploy/L4-furniture-catalog.md`。

已有项目只升级明确指定的 `L4_FurnitureInstances_merged_real_test`（项目 13）。使用既有 `upgrade_furniture_instance_choices` 命令先预览，再按项目名称与配置 SHA-256 应用。该项目原有 28 类，实际配置差异仅为追加 `<Choice value="绿植盆栽" alias="potted_plant"/>`。原配置 SHA-256 为 `47d37de4f4bee36f35bcd1913135141e926934a5b6a69f88ccbd69669235a0cd`，升级后为 `c4559f88ea5971526a267ae99e01ec9ea81028caef1ac9ab29004cd971df6c05`；上线时重新核对。

## 验证

- 家具、Image、界面与保存相关前端测试：234 通过。
- 家具实例后端、模板及配置升级测试：55 通过。
- 聚合、严格 Schema 与导出再导入测试：17 通过，包含盆栽类别往返。
- 生产镜像构建通过。修复旧测试对 XML 固定缩进的假设，仍逐字断言既有 XML 完全保留。
- 独立 QA：`label-studio-potted-plant-qa`，端口 `127.0.0.1:18089`，独立卷 `label-studio-potted-plant-qa-20260911-data`。
- 在最新数据副本中验证按钮可选、实例改类保存及重开。QA 仅将一个测试实例从“其他”改为盆栽，对应 2 条结果变化；几何和 ID 保留、复核转为 pending。其余实例、全部正式标注及参考关系不变；L4 草稿仍为 40 个实例。

配置升级本身不重分类现有标注。若已标为“其他”，由用户选中实例，在“当前实例类别”中选择“绿植盆栽”并应用，随后重新复核。上方类别按钮只影响后续绘制。

## 部署与回退

确认用户保存并暂停编辑后，重新备份数据库、媒体、环境和 Compose；同步更新 8080 的 app、worker，并只追加目标项目选项。上线后核对除项目 13 类别配置外的数据指纹全部一致。QA 数据只作验证，不覆盖正式卷。

尚未生成盆栽结果时，可以核对后恢复原配置及 `l2-entry-7d25b4821` 镜像；已有 `potted_plant` 结果后，必须保留选项和数据，并使用支持 29 类的镜像，不能直接回退到只支持 28 类的版本。具体备份与部署核对记录保存在本地审计目录。
