# L4 分层标注与几何编辑验收 — 2026-09-11

## 版本与范围

- 输入基线：`8a9dd1572cc4613e2561a2bdda87a64e5b1e6761`。
- 功能分支：`feature/l4-hierarchical-focus-and-geometry-ux`。
- 可执行代码：`77f7341e1124e24280cbfd34e23e8ef1b2ef2426`；后续发布记录提交只增加文档。
- 固定镜像：`label-studio-window-l4:hierarchy-77f7341e1`。
- 镜像 ID：`sha256:58477203df229c0003e381270053b29d5ac932e5e51f45cec95d574835b5abf9`。
- 稳定分支：`integration/occupancy-room-window-l4-20260907`。
- 发布标签：`l1-l4-stable-20260911-hierarchical-focus`。

实现 L4 参考标签隐藏、房间/分区/组团 Focus、可通行逻辑区域并集显示、整组轮廓创建实例，以及原图像素坐标的旋转缩放和独立角度适配预览。操作说明见 [L4 分层标注与几何辅助](../l4-hierarchical-annotation.md)。不新增类别、HTTP 路径、数据库迁移或 Schema 版本。

## 自动验证

| 范围 | 结果 |
| --- | --- |
| Editor 全量 | 3715 通过；61 项 ImageView 历史失败；5 项原有跳过 |
| 同环境固定基线 ImageView | 61 项失败名称与本次完全一致，无新增失败 |
| DataManager 全量 | 660 通过 |
| Django windows / occupancy / reference_sync / furniture_instances | 161 通过 |
| 迁移、聚合及 Schema 脚本 | 54 通过 |
| 前端生产构建及最终 Docker 镜像构建 | 通过；保留既有构建警告 |

没有新增跳过、删除断言或更改依赖清单/锁文件。矩形固定边覆盖横图、竖图、正方图，8 个控制点及 0/30/45/近 90/180/312.849/近 360 度；显示变换覆盖 25%、50%、100%、200% 和图像旋转。新增连接显示测试与既有导出规则使用同一套合成 fixtures。

针对性测试包含：并集孔洞与不同逻辑 ID、参考显示缓存、显示/Focus 不写入结果、范围选择保护、轮廓创建及撤销重做、预览取消/应用、来源及保存竞态、晚加载参考控制器订阅。

## 独立真实数据 QA

Compose 项目 `label-studio-l4-hierarchy-qa`；端口 `127.0.0.1:18087`；独立卷 `label-studio-l4-hierarchy-qa-20260911-data`。图片只读挂载。初始 SQLite 在线快照校验通过，SHA-256 为 `9b6f748bd9c387fa1c362ad50ebb59f41554dd55a7fdf322aefd9679c9b4aaeb`。

快照中的正式标注含 31 个实例，最新草稿含 37 个。QA 在副本中完成：

1. 重算原有 L2 过期窗投影，并按既有流程应用 L3/L4 参考，仅用于建立可提交的 QA 来源链；不更改原人工几何。
2. 房间→功能分区→组团选择；客厅三角内部接缝隐藏；相关参考按层级显示；范围外实例点击不改变选择。
3. 旋转沙发适配到 315°，应用后重新复核；已有实例组团复制完整轮廓，得到一个新实例，重复创建被阻止。
4. 整体实例撤销/重做；矩形鼠标启用、完成自动回 Move；三点矩形草稿 Esc 取消。临时手绘实例撤销后不保留。
5. 本组批量确认、从客厅跨组推进到厨房；面包屑同步；最终 38 个实例已复核。
6. 正式提交、重开、导出和重新导入后再次正式提交。最终全部 453 条结果（几何、ID、上下文、复核、服务器来源及只读参考）与导出前正式结果完全一致。
7. 1280×900 窄屏、深色主题及正常宽屏检查。显示设置不写入标注。

与原快照比较，语义数据变化仅包括：L2/L3/L4 正式标注 11/12/13、提交后删除的原草稿 43、参考绑定 2/3、任务 22/23 的参考元数据、预测 8/9。全部对应上述 QA 操作；其他正式标注、草稿、项目配置和参考映射指纹一致。真实图片、导出、数据库及截图只归档在本地 `work/hierarchy-audit`，不提交 GitHub。

关键本地证据：`editor-final.json`、`baseline-comparison.json`、`datamanager-full.json`、`backend-django.log`、`script-tests.xml`、`qa-final.json`、`final-data-diff.json`、`rollback-read.json`、`living-outline-qa.png`、`dark-narrow-qa.png`。

## 部署交接与回退

本记录形成时，8080 的 app 和 reference-sync 仍运行 `lineage-2a066ac15`，均健康。上线须在用户确认已保存并暂停编辑后，重新备份最新数据库、媒体、配置和 Compose，再同时替换 app/worker 镜像。不得以 QA 数据覆盖正式卷。部署后的容器身份、健康与数据核对记录保留在本地审计目录。

原环境已有 L2 任务 20 的分区窗投影过期；本次仅在 QA 副本修复以完成验收。上线不自动修改原 L1—L3 标注，原有来源保护与手动更新流程继续生效。

回退镜像 `label-studio-window-l4:lineage-2a066ac15` 已通过对 QA 38 个新结果的后端几何/复核校验。沿用既有数据契约，回退只换镜像和 Compose，不恢复旧数据库。该旧镜像的启动诊断仍打印原有可选 coverage 模块缺失提示，验证脚本正常完成并返回 0。

验收完成后停止临时 QA 服务，保留数据卷、固定镜像、截图及报告。原稳定标签和回退镜像不覆盖。
