# L4 原父组团更新后的实例恢复

输入版本：`0a17ceddbffd9b2457984a1d0a636f541bacdba3`。代码版本：`86fd3014625a96a512bf8af8e33def35625bd617`。

## 问题与操作

L3 家具组团轮廓变化并手动应用到 L4 后，现有实例保存的父级指纹过期。旧版能够显示 `stale`，但普通复核又拒绝父级指纹不一致的实例，缺少明确接受更新的入口。

选中需处理实例，检查它仍属于原组团且符合新边界，点击 **检查并接受父组团更新**。校验通过后保存草稿并转为待复核，再点击 **已检查，确认复核**，或开始连续复核。接受操作本身不确认复核、不自动定位下一项、不正式提交任务。

只接受原 room_id → zone_id → group_id 对应父级的新版本，不使用当前 Focus 重绑。原父级删除、父级链变化、重复/歧义参考、越界（含孔洞）、跨组、分块重叠、类别/方向/来源损坏等情况继续阻止接受，并显示具体原因。需先处理这些问题。

更新范围仅为目标实例全部几何、类别、方向结果的 `parent_fingerprint`、`review_status=pending` 和 `review_fingerprint=null`。保留 ID、几何、类别、方向、原父级 ID、source_version 和 provenance。未修改 HTTP API、Schema、数据库结构或项目配置。

## 保存与保护

沿用保存草稿 → 检查服务器已应用参考版本 → 固定目标校验及原子写入 → 保存草稿。等待期间选中实例、实例内容、父级或标注发生变化时中止；模型写入异常整体恢复。双击不会重复执行。

后置保存失败时保留本地结果，冻结进度并禁用再次接受/确认；现有“仅重试保存”只持久化已有修改。窗口原始结果备份仍可用。不要通过删除重画或随意移动实例绕过父级复核。

## 验证

- 首轮相关控件、模型、操作会话 104 项通过。
- 最终相关回归 18 套、233 项全部通过，包括父级恢复的 21 个用例、新增控件及 Image 原子恢复/重开测试、既有快速复核、绘制/Move、参考与序列化场景。
- 生产 Docker 构建成功；依赖清单与锁文件没有变化。此次针对局部修复运行相关回归，没有重新运行整个 Editor/DataManager 测试集。
- 从当前 18086 数据卷取得 SQLite online backup，新建独立卷及 Compose，在 `127.0.0.1:18088` 验收。
- 真实副本初始为 31 个实例、439 条结果，其中餐桌为唯一 stale，其余 30 个已复核。
- 浏览器点击接受后变为 30 已复核 / 1 待复核 / 0 需处理；保存重开保持一致。再显式确认餐桌复核，变为 31 已复核，后端完整校验无错误。没有点击正式 Update/Submit。
- 精确比较：只改变餐桌的矩形和配对类别两条结果的三个父级/复核字段。其余结果、全部正式标注、其他草稿、项目配置、参考绑定/映射/审计保持一致（worker 心跳单独排除）。

## 部署与回退

镜像：`label-studio-window-l4:parent-86fd30146`。
镜像 ID：`sha256:28ec2427ccaff03d130deeea1c53632253aacafd59abb5377492be2efe9f1e02`。
OCI revision：`86fd3014625a96a512bf8af8e33def35625bd617`。

使用者已明确确认 18086 保存草稿并暂停编辑。2026-09-09 15:46–15:48（Asia/Shanghai）重新核对容器镜像、数据卷、端口及项目 13 身份，暂停 app/worker，备份最新数据库、卷内其他文件、项目配置和 Compose，再同步更新两服务到上述镜像。

升级后 app healthy、worker running、均为固定镜像且重启计数为 0，HTTP `/health/` 为 UP。全部正式标注、草稿、参考关系/审计及项目配置指纹与升级前一致。刷新原 18086 页面后仍为 30 已复核 / 0 待复核 / 1 需处理；已选中餐桌并确认新按钮可用。没有替使用者接受真实餐桌的父级更新或确认复核。

18088 独立副本完成验收后停止服务，保留数据卷、镜像和证据。升级前备份位于本机 `work/parent-update-audit/before-deploy.sqlite3`、`before-deploy-volume-files.tar.gz`、`compose-before-deploy.yml`，后置比对记录为 `after-deploy.json`。

回退到 `label-studio-window-l4:review-98a1f316f`，使用当前数据卷，保留更新后产生的草稿与复核结果；不要用旧数据库覆盖新数据。18082 与 18085 不在本次更新范围。

本机验收说明、截图位于当前任务 `outputs/l4-parent-update`；详细数据指纹与快照位于 `work/parent-update-audit`。原始研究数据和数据库不提交 Git。
