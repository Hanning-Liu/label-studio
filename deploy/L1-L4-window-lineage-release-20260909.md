# L1—L4 窗来源一致性发布验收

输入：`dd40a0dc3838610639d5b168d1fbd488e003689b`。验收代码：`2a066ac151b272313e52efd0a21b50782949480e`。发布记录提交仅增加本文件，可执行代码与验收镜像一致。

修复分支：`fix/l1-window-inheritance-consistency`。稳定集成分支：`integration/occupancy-room-window-l4-20260907`。新增附注标签：`l1-l4-stable-20260909-window-lineage`；原 `l1-l4-stable-20260909` 保留。

## 行为

- 正式 L1 有窗则逐级完整继承；正式 L1 无窗则允许无窗。断链、取消、多候选、来源/图片/版本异常不按无窗放行。
- 共享来源链校验用于创建、参考应用、正式提交、既有状态接口及只读审计。草稿继续可保存，L4 来源异常时暂停确认及连续推进，不批量改写复核状态。
- 提供带项目身份、配置 SHA-256、来源版本保护的最小窗控件升级命令，以及四层正式标注/配置/绑定/草稿的本地证据导出。
- 完整 L4 聚合必须提供 `--lineage-manifest`。重新校验四层来源、原始文件、窗集合及投影，不信任报告的通过标志。保留 /4、旧格式读取和重新导入能力。
- 真实聚合同时验证并修复了明确来源 ID 与图节点 ID 的对应，以及极小浮点舍入误差；不猜测前缀，不改写标注父级 ID。

操作命令及兼容条件见 [使用与恢复说明](L1-L4-window-lineage.md)。

## 自动验证

| 范围 | 结果 |
|---|---|
| reference_sync、windows、occupancy、furniture_instances 后端 | 161 项通过 |
| 房间/分区转换、家具聚合及 Schema 脚本 | 53 项通过 |
| Editor 全量 | 166 套通过，3,711 项通过；ImageView 61 项历史失败，5 项原有跳过 |
| 固定基线的 ImageView 对照 | 同环境复现 61 项失败；用例名称逐项相同，均为已有 referenceSyncController 空对象错误 |
| DataManager 全量 | 12 套、660 项通过 |
| 最终生产构建 | 通过；没有依赖清单、锁文件、数据库迁移或 Schema 版本变更 |
| 镜像代码核对 | 14 个新增/修改后端文件 SHA-256 与已提交源码一致；app、worker 版本一致 |

未新增跳过项或删除断言。Editor 初次全量缺少一份共享后端几何夹具；补齐只读挂载后重新运行最终全量，剩余失败仅为已确认的 ImageView 历史问题。新增覆盖来源检查与加锁快照之间发生变化时中止，并保留草稿。

## 真实数据副本 QA

从 18086 数据卷在线备份到全新独立卷，SQLite quick_check 通过。只读取源卷，不刷新或提交用户的 18086 窗口。

原始案例仍为 L1 项目9/任务19/标注10 → L2 项目10/任务20/标注11 → L3 项目12/任务22/标注12 → L4 项目13/任务23/标注13。四层各 12 条窗线，窗几何与 window_context 指纹相同。

**新增审计发现原 L2 正式标注的一处旧窗投影过期。** 这不代表漏窗，但按完整标注定义不能直接通过。本轮仅在 QA 副本中，经现有 API 重新提交 L2、手动应用并提交 L3/L4，随后四层完整验收通过。运行中的原始 L2 标注未修复；以后部署新版本时，需要按提示处理。

- 有窗真实副本：31 个家具实例，12 条窗线、10 个窗连接、13 条 L2 投影和16条 L3投影。完整 /4 严格 Schema 通过，62 条家具结果重新导入后逐项一致，所有窗字段保持不变。
- 独立无窗链：四层正式标注有效、旧下游配置无窗控件；31 个实例完整聚合和严格 Schema 通过。页面显示“L1 无窗，窗参考一致”，允许复核。
- 独立异常链：对 L2/L3/L4 分别构造漏窗、多余、重复、上下文和几何变化，连同 L1 删除部分/最后窗、新增首窗场景共18项拒绝验证通过。取消根正式标注后，页面显示明确的 L1 任务入口并禁用复核，原31个已复核状态保持不变。
- 替换证据文件会失败；已有发布输出的 SHA-256 不变。配置升级的预览、幂等、冲突和来源/配置并发保护由事务测试验证。
- 数据保护：原源环境的10份正式标注、2份草稿、7份项目配置和3个参考绑定均未改变。QA副本中只有明确测试的正式标注11/12/13及绑定2/3更新；原草稿、其余原标注、原配置和已有审计记录一致。独立测试链和QA页面草稿单独记录。

本地完整证据位于本任务 `work/lineage-audit`；用户报告与3张页面截图位于 `outputs/window-lineage`。研究数据、数据库、凭证和截图不提交 GitHub。

## 镜像、环境和回退

镜像：`label-studio-window-l4:lineage-2a066ac15`。

镜像 ID：`sha256:3d679686a8823dddb3a926dac49d18b21f9262a9a14111267b7b1699d6265b8a`。

QA Compose 项目 `label-studio-window-lineage-qa`，独立卷 `label-studio-window-lineage-qa-data`，端口 `127.0.0.1:18087`，图片只读。app/worker 使用同一镜像。验收完成停止本次 QA 服务，保留镜像、数据卷和证据。

本轮发布代码，不升级18086；它继续运行 `parent-86fd30146`。18082 的 `catalog-55a6b701c`、18085及其余原服务保持原版本。没有向镜像仓库上传镜像。

原稳定标签是回退点。代码撤销采用新 revert 提交、重新验证和新标签，不重写已发布历史，不以旧数据库覆盖后续标注。后续部署需先保存窗口并重新备份；本发布不包含运行环境升级。
